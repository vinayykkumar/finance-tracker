/**
 * Glossary slice (080): modular copy for future i18n / tooltips.
 * @see README in parent folder.
 */
export const termId = "080" as const;
export const termEn = "withholding" as const;
export function describeTerm(): string {
  return termEn;
}
