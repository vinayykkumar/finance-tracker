{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // node --test runs *.test.ts files directly and needs explicit ".ts"
    // specifiers (Node ESM resolution); tsc only ever runs with --noEmit here.
    "allowImportingTsExtensions": true
  }
}
