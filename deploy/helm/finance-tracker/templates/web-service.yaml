apiVersion: v1
kind: Service
metadata:
  name: {{ include "finance-tracker.fullname" . }}-web
  labels:
    {{- include "finance-tracker.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.webPort }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "finance-tracker.selectorLabels" (dict "root" . "component" "web") | nindent 4 }}
